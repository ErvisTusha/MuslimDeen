name: muslim_deen
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  adhan_dart: ^1.1.2
  geolocator: ^14.0.2
  flutter_compass: ^0.8.1
  hijri: ^3.0.0
  shared_preferences: ^2.2.3
  geocoding: ^4.0.0
  http: ^1.5.0
  url_launcher: ^6.3.2
  flutter_local_notifications: ^19.4.2
  logger: ^2.6.2
  timezone: ^0.10.0 # For notification scheduling
  # Removing flutter_native_timezone as it's causing JVM target conflicts
  # We can get device timezone using DateTime.now().timeZoneName instead
  latlong2: ^0.9.1
  get_it: ^8.2.0
  flutter_timezone: ^5.0.0
  flutter_riverpod: ^3.0.3
  riverpod: ^3.0.3
  permission_handler: ^12.0.1
  audioplayers: ^6.5.1
  home_widget: ^0.8.0
  workmanager: ^0.9.0
  vibration: ^3.1.4
  sqflite: ^2.4.1
  path: ^1.9.0
  sqflite_common_ffi_web: ^1.0.1+1

  intl: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  mockito: ^5.4.6
  build_runner: ^2.4.15
  clock: ^1.1.2
  sqflite_common_ffi: ^2.3.0
  integration_test:
    sdk: flutter

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/muslimdeen_icon.png"

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/audio/
